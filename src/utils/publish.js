import fs from './fs';
import path from 'path';
var webpack = require('webpack');
var MemoryFS = require('memory-fs');

async function zipFolder (entryPath, outputPath) {
  // todo: zip a folder
}

async function rsyncFolder (entryPath, outputPath) {
  // todo: rsync a folder to a remote machine
  // ues rsync account
}

export default { zipFolder };




